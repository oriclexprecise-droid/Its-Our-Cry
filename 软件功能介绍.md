# It's Our Cry!!!!! 配音工作台

**给每个角色一个会呼吸的声音：剧本分析 → 情绪校对 → 多角色配音 → 合成字幕，一条龙完成。**

It's Our Cry!!!!! 是一款面向 MyGO!!!!! / Ave Mujica 同人二创的本地配音工作台。你只需要把剧本粘贴进来，剩下的交给 AI 和本地语音合成：自动分析情绪、自动匹配角色与参考音频、生成多角色配音，最后输出合并音频、分角色音轨和 SRT 字幕。

---

## 一、开箱即用的创作体验

- 桌面应用，点开即用，不需要懂代码、不需要手动配环境。
- 安装包支持自选目录，不强制安装到 C 盘，卸载不污染系统。
- 程序自带完整角色模型、参考音频、背景图与开屏动画，核心素材全部离线内置。
- 启动即进入创作界面，无需登录、无需联网（仅 AI 分析与整合包下载需要网络）。

## 二、AI 驱动的剧本分析

- 支持标准剧本格式：**角色名：台词**，自动识别正文与旁白。
- 接入 DeepSeek 等大模型，为每句台词标注 11 类情绪：生气、告别、哭泣、感动、决心、悲伤、认真、害羞、微笑、惊讶、思考。
- **AI 角色名纠错**：打错角色名时自动指出错误行、给出修正建议，你确认后可直接修正，无需重新分析。
- **中/日双语创作**：选择日语时，AI 会把台词翻译成日语用于配音，字幕仍然保留中文。

## 三、人机协同的情绪校对

- 每句台词都能单独修改情绪，AI 建议只是起点，最终决定权在你。
- 每条台词可自定义前间隔时间，也支持全选、多选后批量统一设置。
- 生成完成后每条语音都能试听，不满意单独重新生成，不用整段重来。
- 实时进度条与一键取消，长剧本也能放心跑。

## 四、专业级多角色语音合成

- 内置 13 个角色模型：千早爱音、要乐奈、高松灯、椎名立希、长崎素世，以及 Mortis、三角初华、八幡海铃、祐天寺若麦（乖猫/哈气）、若叶睦、丰川祥子（白/黑）。
- 模型激活词可自由增删，AI 纠错也会同步识别你自定义的称呼。
- 按“角色 + 情绪”自动匹配参考音频，同一个角色不同情绪能演出不同语气。
- 缺少模型或参考音频时不会中断：对应台词自动降级为纯字幕，其余语音正常生成。

## 五、一站式的成品导出

- 自动生成合并音频与 SRT 字幕，字幕与语音严格对齐。
- 支持**按角色拆分音轨**，每个角色独立一条 WAV，直接适配剪辑软件的时间轴。
- 旁白无需配音，字幕时长可自动估算，也可固定时长。
- 每次导出生成独立文件夹，结果位置清晰，一键打开，历史导出互不覆盖。

## 六、小白友好的部署工具

- 一键环境扫描：系统、CPU、内存、显卡、CUDA、依赖包、FFmpeg、角色模型，缺什么一目了然。
- 自动识别 RTX 50 系列与旧显卡，推荐对应版本的 GPT-SoVITS 整合包，支持程序内下载、解压、取消。
- 一键 pip 安装缺失依赖，一键补齐角色模型，一键下载 FFmpeg。
- 内置 7-Zip / WinRAR / tar 多套解压方案，遇到复杂压缩包也能解。
- 空间清理工具：扫描可清理的冗余模型副本、日志、构建缓存，二次确认后才删除。

## 七、灵活开放的 AI 配置

- 不绑定单一厂商：支持自定义 API 地址、模型名与接口名称，可接入任何兼容 OpenAI 协议的模型。
- 内置 DeepSeek 一键预设，填好 Key 即可使用。
- API Key 使用 Windows 加密存储，界面只显示掩码，支持一键清除。

## 八、为后续优化沉淀数据

- 内置日志面板：AI 情绪建议、你的手动修正都会记录在案，可查看、下载、重置。
- 每一条“AI 建议 → 用户修改”都是最有价值的标注数据，为后续调优提示词、微调模型铺路。

## 九、预设与素材共享

- 纠音词典、情绪参数模板、脚本情绪映射均可单独导出为 JSON，导入后立即覆盖本地对应配置，方便创作者互相分享配音风格。
- 设置中设有独立「导入导出」板块，集中管理纠音词典、情绪参数模板、脚本情绪映射三类预设，以及参考音频库整体打包。
- 参考音频库 + 情绪管理支持整体打包为 ZIP，包含全部音频、字幕与情绪列表；导入时自动解压合并，同名文件会覆盖。
- 所有导出文件均为标准 JSON / ZIP 格式，不依赖任何网络服务，可离线互相传递。

## 十、清晰的安全与版权声明

- 明确标注非商业、个人创作用途，尊重角色、模型与参考音频的原始版权。
- 本地运行、隐私友好：剧本内容不会上传，只有匿名修正日志用于改进效果。
- 内置 B 站主页与 QQ 群入口，方便同好交流与反馈。

---

**版本：2.1.0 | 开发者：ORiCale**

*从一句台词到一部作品，It's Our Cry!!!!! 陪你把热爱变成声音。*
