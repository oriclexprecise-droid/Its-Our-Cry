# It's Our Cry!!!!! 群聊机器人知识库

> 本文件用于导入 AstrBot 知识库，让群聊机器人回答群友关于 It's Our Cry!!!!! 配音工作台的问题。
> 回答要求：口语、简短、给步骤，把用户当彻头彻尾的零基础小白；不知道的不要编，引导用户导出日志包。

## 一、这个软件是什么

- 一款给 MyGO!!!!! / Ave Mujica 同人二创做配音的本地桌面工具，免费使用。
- 把剧本粘贴进去，软件负责：AI 标情绪（可选日语翻译）→ 用本地 GPT-SoVITS 合成每个角色的语音 → 导出合并音频、分角色音轨、SRT 字幕，或 WebGAL 游戏脚本工程。
- 剧本和语音都在本地处理；只有“AI 分析情绪 / 日语翻译 / 参数建议”会调用第三方 API（默认 DeepSeek 等 OpenAI 兼容接口）。
- 面向不会编程、不会配环境的普通用户，点开即用。

## 二、新手三步开跑

1. 安装或更新
   - 新用户：双击安装包，安装目录可以自己选，不强制装到 C 盘。
   - 老用户：用增量更新包覆盖安装即可，不用重新下载角色模型，本地设置会保留。
2. 部署 GPT-SoVITS（不装不能生成语音）
   - 打开软件 → 右上角“设置” → “部署”。
   - 已安装 GPT-SoVITS：填写目录 → 点“扫描环境”。
   - 未安装：按自己的显卡选择整合包 → 选解压目录 → 开始下载 → 完成后再次扫描。
   - 部署板块会一次检查依赖、FFmpeg、模型等缺什么。
3. 填 API Key（AI 分析情绪 / 日语翻译 / 参数建议用）
   - 设置 → 通用设置 → API 设置。
   - 可点“DeepSeek 预设”，填入 API Key 后“保存配置”。
   - Key 只在本机加密保存，可随时“清除 Key”。

## 三、首页怎么操作

- 新建项目：输入项目名称 → 选“SRT 工作台”（给 AE / Pr 剪辑用）或“WebGaL 板块”（给 WebGAL 游戏用）→ 再选“API 生成”或“客户端生成”。
- 往期项目：点开可继续做；SRT 和 WebGAL 的项目分开保存，同名项目会提示不能重复。
- 版本历史：工作区右上角按钮，可手动“保存版本”、载入小版本、删除小版本。
- 设置：齿轮按钮。首页进的设置，返回时回首页；工作区进的设置，返回时回工作区。
- 退出到主页：会先询问是否保存当前草稿。

## 四、SRT 工作台（适配 AE / Pr 剪辑）

### 第 1 步：输入剧本
- 格式：角色名：台词，例如：千早爱音：大家好！
- 支持“旁白”和“角色名（心理活动）”写法。
- 配音语言选中文或日语；选日语后，解析 / 分析会自动翻译成日文配音，字幕仍保留中文。
- API 模式：点“分析情绪”。客户端模式：点“客户端生成”生成提示词。
- 勾选“强制重新分析”可忽略已有结果重新跑一遍。

### 第 2 步：校对台词与情绪
- 每行都能单独改：角色、台词、前间隔（秒）、情绪。
- 改了单条后可以只重新分析这一条，不用整段重来。
- “选择模式”可全选 / 多选，批量设置前间隔。
- 撤销 / 重做是全局的，输入剧本和校对都算。

### 第 3 步：生成语音
- “生成全部语音”：按角色 + 情绪匹配参考音频，合成整段配音。
- 没有模型或没有参考音频的行不会中断流程：会“仅保留字幕”，并告诉你原因。
- “只生成 SRT 字幕”：只在出现“某些角色没有模型”时出现；有模型的行仍生成语音，没素材的行只留字幕，先把结果跑出来。
- 支持逐条“试听”和“重新生成”，可以随时取消生成。

### 第 4 步：导出
- 填写作品名称 → 导出。
- 成品保存到“设置 → 通用设置 → 作品保存路径”下的 srt客户端 / srt api端 文件夹。
- 一次导出包含：合并音频、分角色音轨、SRT 字幕。
- 作品名不能包含 \ / : * ? " < > | 这些符号，也不能以 . 或空格结尾；出错时会提示具体原因。

## 五、WebGaL 板块（给 WebGAL 游戏配音）

1. 把 anogo 导出的“无配音脚本”整段粘贴进来。
2. 点“解析脚本”，软件自动识别对话、角色、figureId 和表情。
3. 处理情绪：
   - API 模式：点“可选：AI 分析情绪”。
   - 客户端模式：点“生成分析提示词”或“生成翻译提示词”，复制去 AI 客户端，把返回的 JSON 粘贴回来，点“应用 AI 结果”。
4. 日语模式：配音语言选日语后，解析 / 分析会自动翻译，不需要单独翻译按钮。
5. 校对：可改角色、台词、情绪；可开启“心理活动配音”并选择配给哪个角色；路人角色（老师、同学等）不会生成音频。
6. 生成全部语音，然后导出：
   - 完整工程保存到作品保存路径的 webgal客户端 / webgal api端。
   - 纯语音可另选游戏音频目录：WebGaL 启动器目录 → public → games → 你的游戏名 → game → vocal，把 vocal 路径粘贴进去或点按钮选择。
   - 生成的 script.txt 可以一键复制。

## 六、API 生成和客户端生成怎么选

| 项目 | API 生成 | 客户端生成 |
| --- | --- | --- |
| 怎么工作 | 程序直接调大模型 | 生成提示词，你复制到 AI 客户端，把结果粘贴回来 |
| 优点 | 一键、省事 | 省钱，长剧本不心疼，可用自己的客户端 |
| 缺点 | 花钱、受网络影响 | 需要手动复制粘贴几步 |

客户端生成操作步骤：
1. 点“生成提示词”（WebGAL 是“生成分析提示词 / 生成翻译提示词”）。
2. 点“复制提示词”，去 DeepSeek 等客户端粘贴发送。
3. 把 AI 返回的 JSON 完整复制。
4. 回到软件，粘贴到“粘贴 AI 返回的 JSON 结果”，点“应用 AI 结果”。

长剧本会分段：默认每段 100 行（设置 → 通用设置 → 客户端生成可改为 10-500 行）。逐段发给 AI，最后一段会要求 AI 把所有段合并成一份完整 JSON 返回，避免被输入长度限制截断。

## 七、设置板块逐个说明

1. 部署：检查 / 下载 / 解压 GPT-SoVITS，扫描环境，安装缺失依赖。
2. 模型配置：内置 13 个角色模型：千早爱音、要乐奈、高松灯、椎名立希、长崎素世、Mortis、三角初华、八幡海铃、祐天寺若麦乖猫、祐天寺若麦哈气、若叶睦、丰川祥子白、丰川祥子黑。
   - 可以添加自定义角色 / 模型（SoVITS 的 .pth + GPT 的 .ckpt），自定义角色和原生角色一样支持参考音频、情绪管理、生成。
   - 激活词可自由增删；系统自带角色不能删除。
3. 语音与情绪：
   - 纠音词典（日语模式）：中文 / 昵称 → 日文假名，日语配音时使用。
   - 参考音频库：按“角色 + 情绪”管理参考音频；上传时除了选音频文件，还要填写这段音频对应的字幕文本。
   - 情绪管理：可增删情绪，会同步 AI 分析的可选范围和语音库；可一键恢复系统默认。
   - 情绪参数模板（SoVITS）：按情绪设置温度、top_k、top_p、语速、seed；勾选“是否启用当前参数设置”才生效；可保存 / 载入 / 删除预设；AI 可以生成参数建议。
4. 导入导出：
   - 参数预设（JSON）：纠音词典、情绪参数模板、脚本情绪映射，可单独导出 / 导入。
   - 参考音频库 + 情绪管理（ZIP）：打包全部参考音频、字幕、情绪列表；导入自动解压合并，同名文件会被覆盖。
   - 非原生角色包（ZIP）：导出所选非原生角色的模型、参考音频、参考文本、激活词、情绪，默认全选；导入时若包内角色已存在，整个包会被拒绝并提示。
5. WebGaL 设置：
   - 脚本情绪映射：解析 anogo 脚本时以 changeFigure 的 -expression 表情为准，缺失时回退 -motion；表情词映射到系统情绪后用于选参考音频，导出的脚本仍保持原样。
   - 情绪分析与翻译：日语模式下可关闭“分析情绪时重新翻译全部台词”，保留已有译文只补缺失，更省 API。
6. 清理空间：扫描可清理的冗余模型副本、日志、构建缓存，二次确认后删除。
7. 日志：可查看、下载、重置；遇到问题点“导出日志包”，把 zip 发给开发者最快定位。
8. 声明：用户协议、免责声明、隐私说明、共享义务、感谢名单。
9. 通用设置：
   - API 设置：AI 名称、API 地址、模型名称、API Key，DeepSeek 预设，清除 Key。
   - 旁白字幕时长：自动估算或固定时长。
   - 项目与版本：生成完成后自动保存项目；自动保存小版本（间隔 1-120 分钟，默认 5 分钟）；最多保留项目数（10-500，默认 50）；每个项目最多保留小版本数（5-200，默认 50）。
   - 作品保存路径：导出统一保存到这里，自动创建四个模式文件夹：srt客户端 / srt api端 / webgal客户端 / webgal api端；不设置会提醒。
   - 客户端生成：每段提示词台词行数（10-500，默认 100）。
   - 外观：背景图选择或随机。
   - 性能：低性能模式（逐条生成，内存占用更低，速度较慢，适合低配置电脑）。

## 八、常见问题 FAQ

### 安装与启动
- 问：必须要装 GPT-SoVITS 吗？
  答：要。它是本地配音引擎，不部署就生成不了语音。到“设置 → 部署”按提示操作即可。
- 问：装好之后还需要 API Key 吗？
  答：只有 AI 分析情绪、日语翻译、参数建议需要。不想花钱可以用“客户端生成”，把提示词复制到 AI 客户端，再把结果粘贴回来。
- 问：更新会覆盖我的设置吗？
  答：覆盖安装 / 增量更新会保留本地设置、模型和参考音频。建议更新前先保存一个版本。
- 问：软件打不开或报错怎么办？
  答：先重启软件；还不行就到“设置 → 日志 → 导出日志包”，把 zip 发给开发者。

### 生成与试听
- 问：提示“生成失败（仅保留字幕）”是什么意思？
  答：这一条没生成出音频，只保留字幕。原因会在后面写出来，比如缺模型、缺参考音频或推理失败。
- 问：提示“角色『xx』没有配音模型”或“角色不存在”？
  答：剧本里的角色名没对上软件里的角色模型。检查角色名是否写错、用了没添加的昵称，或到“模型配置”确认角色已安装。WebGAL 解析脚本后会直接标出未知角色。
- 问：提示“缺少参考音频：xx『xx』”？
  答：该角色该情绪还没有参考音频。到“语音与情绪 → 参考音频库”上传音频并填写对应字幕；缺失时会自动降级为纯字幕。
- 问：生成完试听没声音 / 刚生成完不能试听？
  答：先等生成进度完全结束；单条重新生成后通常就能试听。仍不行就导出日志包反馈。
- 问：生成很慢怎么办？
  答：本地合成本来就吃显卡 / CPU。低配置电脑可在“通用设置 → 性能”开启“低性能模式”；长剧本也可以分批生成。

### AI 分析与翻译
- 问：分析很慢、很贵怎么办？
  答：API 按调用次数和 tokens 计费。省钱方式：用“客户端生成”、长剧本分段、开启缓存（相同内容不重复调 API）、日语模式下关闭“重新翻译全部台词”。
- 问：为什么连续失败两次就不调了？
  答：防止 API 异常时一直烧钱重试。连续失败 2 次会自动停止并提示，之后可以再试。
- 问：日语翻译失败怎么办？
  答：检查网络和 API Key / 模型；也可以改用客户端生成翻译提示词。失败的行会标出来，可以单独重试。
- 问：纠音词典为什么不生效？
  答：规则是：台词整句与词条左侧完全相等时，日语直接使用右侧假名；不是整句时，按“长词条优先”做包含替换，例如“soyorin早上好”会把 soyorin 替换成 そよりん。心理活动带括号时也会先去掉括号再查。确认词条左侧写的是剧本原文，并点“保存纠音词典”。
- 问：AI 用量面板在哪？
  答：工作区右侧“AI 用量”按钮，点击展开，能看到 API 调用次数、缓存命中、失败句、输入 / 输出 tokens、估算费用；可刷新、重置会话、清空缓存。估算费用以实际 API 账单为准。

### 默认纠音词条
以下为出厂自带的纠音词典（可增删、可恢复默认）：

| 中文 / 昵称 | 日文发音 |
| --- | --- |
| 长崎素世 | 長崎そよ |
| soyo | そよ |
| soyorin | そよりん |
| soyo酱 | そよちゃん |
| 高松灯 | たかまつともり |
| 灯 | ともり |
| tomorin | ともりん |
| 小灯 | ともりちゃん |
| 千早爱音 | ちはやあのん |
| 爱音 | あのん |
| 小爱音 | あのんちゃん |
| 椎名立希 | しいなたき |
| 立希 | たき |
| 小立希 | たきちゃん |
| rikki~ | りっきー |
| 要乐奈 | かなめらーな |
| 乐奈 | らーな |
| 小乐奈 | らーなちゃん |

### WebGaL
- 问：anogo 是什么？
  答：一个把剧本转成 WebGaL 脚本的工具 / 编辑器。它不负责配音，所以本软件负责配音：把 anogo 导出的无配音脚本粘贴进来即可。
- 问：情绪映射能自己改吗？
  答：能。在“设置 → WebGaL 设置 → 脚本情绪映射”增删；表情词映射只用来选参考音频，导出的脚本保持原样。
- 问：WebGaL 日语配音怎么弄？
  答：配音语言选日语，解析 / 分析时自动翻译，不需要单独翻译按钮。
- 问：WebGaL 导出到哪里？
  答：完整工程到作品保存路径下的 webgal客户端 / webgal api端；只想把音频放进游戏时，选游戏目录里的 public/games/你的游戏名/game/vocal。

### 保存与导出
- 问：作品保存路径是什么？
  答：在“设置 → 通用设置 → 作品保存路径”设置。不设置就不能导出；设置后会自动创建四个模式文件夹。
- 问：导出提示文件名包含非法字符？
  答：作品名不能包含 \ / : * ? " < > |，也不能以 . 或空格结尾；软件会具体提示哪里不对，比如多了一个斜杠。
- 问：小版本是什么？和自动保存什么关系？
  答：小版本是项目快照，保存当前剧本、校对和生成状态。可以手动“保存版本”，也可以开启自动保存（默认每 5 分钟）。在“版本历史”里载入或删除。项目和小版本存在程序目录的固定位置，不能修改。
- 问：撤销 / 重做管哪些？
  答：是全局的：输入剧本、校对、单条修改都算；切换语言、载入版本、新建项目会重置。分析中或生成中不能撤销，会提示先停止。
- 问：导出到指定文件夹？
  答：在导出框输入作品名即可，会自动进入保存路径下对应的模式文件夹。

### 素材与共享
- 问：参考音频怎么添加？
  答：设置 → 语音与情绪 → 参考音频库 → 上传，选音频文件并填写这段音频对应的字幕文本。
- 问：怎么把角色或素材发给别人？
  答：设置 → 导入导出：参数预设导 JSON，参考音频库导 ZIP，非原生角色导角色包 ZIP，对方导入即可。
- 问：导入别人的角色包失败？
  答：如果包内角色在本机已存在，整个包会被拒绝，防止覆盖配置。先删除或改名已有角色，或让对方只导出没重复的角色。
- 问：必须无偿分享吗？
  答：是的。使用本应用有共享义务：自己做的预设（情绪参数、纠音词典、脚本情绪映射、参考音频、角色配置等）应无偿分享给社区，不得售卖。

## 九、回答注意事项（给机器人）

- 回答要简短、友好、给步骤，用户不懂就问。
- 只回答软件功能范围内的问题，不要编造没有的功能。
- 遇到报错先让用户重启软件，不行就“导出日志包”发给开发者。
- 不承诺生成效果；AI 分析和语音合成结果都可能存在误差。
- 费用类问题以实际 API 账单为准。

## 十、联系与反馈

- B 站：https://space.bilibili.com/3493294730381924
- QQ 群：https://qm.qq.com/q/8DXSwA7ZoQ
